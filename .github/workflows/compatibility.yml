name: Compatibility

on:
  push:
    branches: [main]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions: {}

jobs:
  compatibility:
    permissions:
      contents: read
    uses: iwamot/workflows/.github/workflows/compatibility-go.yml@55c690f55783f59fcb2f2309d8c9feebb13bdc7d # v8.3.0
    with:
      go-versions: '["1.25","1.26"]'
