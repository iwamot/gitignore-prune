name: Compatibility

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions: {}

jobs:
  compatibility:
    permissions:
      contents: read
    uses: iwamot/workflows/.github/workflows/compatibility-go.yml@15e638a17daad208034700f2429ee13db2452ce4 # v4.2.0
    with:
      go-versions: '["1.25","1.26"]'
