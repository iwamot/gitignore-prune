name: Compatibility

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions: {}

jobs:
  compatibility:
    permissions:
      contents: read
    uses: iwamot/workflows/.github/workflows/compatibility-go.yml@3a106f414b2ee346211a9c9050a27e2f26336796 # v7.0.0
    with:
      go-versions: '["1.25","1.26"]'
