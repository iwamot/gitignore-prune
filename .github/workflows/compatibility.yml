name: Compatibility

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions: {}

jobs:
  compatibility:
    permissions:
      contents: read
    uses: iwamot/workflows/.github/workflows/compatibility-go.yml@ae5512be479f765d6f3f89302516a6f5c0add845 # v4.1.3
    with:
      go-versions: '["1.25","1.26"]'
