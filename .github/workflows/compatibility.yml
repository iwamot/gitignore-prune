name: Compatibility

on:
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions: {}

jobs:
  compatibility:
    permissions:
      contents: read
    uses: iwamot/workflows/.github/workflows/compatibility-go.yml@ae85e64823f8d8316a33fa549c7a5ec13ab56795 # v8.6.1
    with:
      go-versions: '["1.25","1.26"]'
