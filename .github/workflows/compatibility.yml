name: Compatibility

on:
  push:
    branches: [main]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions: {}

jobs:
  compatibility:
    permissions:
      contents: read
    uses: iwamot/workflows/.github/workflows/compatibility-go.yml@1d30033350f397a683dfdf04708d1a510969e9ed # v8.0.0
    with:
      go-versions: '["1.25","1.26"]'
