name: Compatibility

on:
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions: {}

jobs:
  compatibility:
    permissions:
      contents: read
    uses: iwamot/workflows/.github/workflows/compatibility-go.yml@f4bb8660a94f68c9ac2228d5fad6b990adc922ff # v8.6.0
    with:
      go-versions: '["1.25","1.26"]'
