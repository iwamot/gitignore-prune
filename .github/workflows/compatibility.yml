name: Compatibility

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions: {}

jobs:
  compatibility:
    permissions:
      contents: read
    uses: iwamot/workflows/.github/workflows/compatibility-go.yml@ebf1be0e9a042e490eef4062613562225d4d206f # v5.0.0
    with:
      go-versions: '["1.25","1.26"]'
