name: Compatibility

on:
  push:
    branches: [main]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions: {}

jobs:
  compatibility:
    permissions:
      contents: read
    uses: iwamot/workflows/.github/workflows/compatibility-go.yml@52f24a2038979c4c1ec1cb3c3195ef16bb567d2a # v8.0.1
    with:
      go-versions: '["1.25","1.26"]'
