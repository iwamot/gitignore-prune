name: Compatibility

on:
  push:
    branches: [main]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions: {}

jobs:
  compatibility:
    permissions:
      contents: read
    uses: iwamot/workflows/.github/workflows/compatibility-go.yml@873e99243e0ab9f834bb4eb42198f701bfe5c21c # v8.4.0
    with:
      go-versions: '["1.25","1.26"]'
