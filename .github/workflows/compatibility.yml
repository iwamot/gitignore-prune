name: Compatibility

on:
  push:
    branches: [main]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions: {}

jobs:
  compatibility:
    permissions:
      contents: read
    uses: iwamot/workflows/.github/workflows/compatibility-go.yml@086f2d88815451da2cba7dc9e6e044d86c66743c # v8.2.0
    with:
      go-versions: '["1.25","1.26"]'
